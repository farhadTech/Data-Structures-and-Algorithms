#include <bits/stdc++.h>
using namespace std;
using ll = long long;

void solve(){
    string s;
    s = "abcd";
    string r = reverse(s.begin(), s.end());
}

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int t; cin >> t;
    while(t--){
        solve();
    }
    return 0;
}
